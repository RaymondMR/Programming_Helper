Sí, arrancamos creando un Dockerfile (multi-stage, para compilar assets y dejar solo lo necesario en producción) y un docker-compose.yml que levante al menos:

un contenedor PHP-FPM (la app),
un contenedor web (Nginx) que sirva public/,
y un contenedor PostgreSQL (a menos que quieras seguir usando tu instancia externa).
Propongo este flujo paso a paso:

1.-Dockerfile: multi-stage (node + composer para build, y después imagen final basada en php:8.x-fpm con extensiones y el código listo).
2.-docker-compose.yml: definir servicios app, web, db, redes y volúmenes.
3.-Archivos extra: nginx.conf, variables de entorno específicas, carpeta docker/ si quieres mantenerlo ordenado.
4.-Build + pruebas: docker compose build, docker compose up -d, correr migraciones, etc.
5.- Documentar comandos para levantar/parar y, si quieres, scripts de deploy.
¿Confirmamos que quieres esa estructura (con contenedor de Postgres incluido) y paso al primer archivo (Dockerfile)?


Ya ejecute:
docker compose build y docker compose up -d, pero al ejecutar docker compose exec app php artisan migrate --force
me arroja este error:
In Application.php line 754:

In Application.php line 754:
  Class "NunoMaduro\Collision\Adapters\Laravel\CollisionServiceProvider" not found

que puede estar m,as y como corregirlo ?

http://localhost:8080

Al ejecutar docker compose exec app php artisan migrate --force
 me arroja estos errores:

PS C:\Users\MONCHO\Documents\Laravel Projects\youtube-laravel-demo> docker compose exec app php artisan migrate --force

In Connection.php line 760:

  SQLSTATE[08006] [7] connection to server at "db" (172.18.0.2), port 5432 failed: FATAL:  database "youtube_database" does not exist (SQL: select * from information_schema.tables where table_catalog = yout
  ube_database and table_schema = public and table_name = migrations and table_type = 'BASE TABLE')


In Connector.php line 70:

  SQLSTATE[08006] [7] connection to server at "db" (172.18.0.2), port 5432 failed: FATAL:  database "youtube_database" does not exist

Lo que me indica que no existe la database: "youtube_database"

Ejecuta los comando necesarios para crear la base en el contenedor db
ejecuta un curl http://localhost:8080/ y checa los errores, corrígelos y explícame porque sucedieron
